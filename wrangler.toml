# Delete this file if using Cloudflare Pages
# This is only for Cloudflare Workers (not recommended for static sites)

name = "focushub"
compatibility_date = "2024-01-01"

# For static site deployment via Workers
main = "workers-site/index.js"

[site]
bucket = "./dist"
