// Firebase configuration stub
// Currently not using Firebase backend - all data stored in localStorage

export const initializeFirebase = () => {
  // Stub function for future Firebase integration
  console.log('Firebase stub initialized');
};
